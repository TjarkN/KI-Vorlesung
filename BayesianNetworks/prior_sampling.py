from bayesian_network import BayesianNetwork, BayesianNode

def prior_sample(x,n,bn:BayesianNetwork):
    node = bn.start_nodes[0]
    sample =[]
    for i in range(n):
        sample.q
        while nodes != None:
            x.append(child.get_sample(child.cpt))
    for child in node.childs:
        child = child.get_sample(child.cpt)
    return xi